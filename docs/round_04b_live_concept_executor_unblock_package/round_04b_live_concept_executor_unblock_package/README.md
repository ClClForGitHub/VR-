# Round04B Package: Live Concept Executor Unblock

Use this package after Round04 reported all live sample cases as blocked at concept generation.

Primary goal: implement the missing real concept-image execution boundary for natural-language + reference-image samples, then run one canary case through live concepts and downstream stages if the concept artifacts exist.

Start from branch: `round04-live-user-samples-full-flow`.
Create branch: `round04b-live-concept-executor`.

Main execution file: `01_Codex_task_packet.md`.
