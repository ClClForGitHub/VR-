# Third Party Sources

This directory is for local external checkouts used by the Blender/agent
runtime, such as Blender MCP adapters or add-on source trees.

The actual cloned repositories are ignored by Git. Record durable integration
decisions in `docs/` and keep source provenance there when a dependency becomes
part of the project contract.
