# Blender Add-ons

This directory stores local Blender add-on archives or unpacked add-ons used for
manual/runtime experiments.

Large add-on packages are ignored by Git. Keep install commands, provenance, and
runtime notes in `docs/` when an add-on becomes required.
